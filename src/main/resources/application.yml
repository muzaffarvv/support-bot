
bot:
  token: ####-####-####-####-####
  username: ####-####

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bot_db
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: support-bot
